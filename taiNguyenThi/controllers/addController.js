window.listController = function($scope,$http,$location){
    var url = "http://localhost:3000/shoes"

    $scope.onAdd = function(){
        var valid = false;

        if(!$scope.inputValue || !$scope.inputValue.id || $scope.inputValue.id <0)zz

    }
}